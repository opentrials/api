{
  "name": "opentrials-api",
  "description": "API for OpenTrials.net",
  "scripts": {
    "postdeploy": "npm run migrate && npm run seed && npm run reindex"
  },
  "env": {
  },
  "addons": [
    "heroku-postgresql",
    "searchbox"
  ],
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ]
}
